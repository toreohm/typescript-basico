/*Con el comando "npx tsc src/hello.ts" puedes convertir archivo *.ts a *.js
Si no funciona se puede ejecutar estos dos comandos:
1)tsc --init  Esto creará el archivo tsconfig.json
2)“npx tsc -p ./ -w  Esto compilará en el mismo directorio todos tus archivos .ts */

const myName = "Hector";
console.log(myName);
