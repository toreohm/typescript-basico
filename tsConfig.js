/*Creando un archivo TSConfig.json - npx tsc --init*/
/*Nuestro código TypeScript se transpilará según las propiedades indicadas en nuestro archivo
TSConfig.json: npx tsc */
/*Compilación en tiempo real: npx tsc --watch */
/*Activamos las siguientes propiedades dentro del archivo tsconfig.json
1)"outDir": "./dist"
2)"module": "commonjs"
3)"rootDir": "./src"*/
